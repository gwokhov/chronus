page {
  background-color: #fcfcfc;
}

page::before {
  content: '';
  position: fixed;
  background-image: linear-gradient(to bottom, rgba(0, 0, 0, 0.02) 0rpx, transparent 12rpx);
  top: 0;
  left: 0;
  width: 100%;
  height: 12rpx;
}

button::after {
  border: none;
}